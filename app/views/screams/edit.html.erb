<h1>Editing Scream</h1>

<%= render 'form', scream: @scream %>

<br>

<%= link_to 'Show', @scream, class: 'btn btn-primary' %>
<% if current_user.role <= 2 %>
<%= link_to 'Delete', @scream, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %>
<% end %>
<%= link_to 'Back', screams_path, class: 'btn btn-secondary' %>

<br>
<br>
<br>